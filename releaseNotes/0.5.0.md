* Fix complex parameter parsing
* Add support for SQL parsing
* Filter out very large files (configurable)
* Add Python and PHP support
* Refactor analyzers to use shared `TreeSitterHelper` utilities
  * Add `extractName` helper for consistent name extraction across analyzers
  * Consolidate `identifyNestedNodes` for nested class detection
  * Reduce code duplication in PHP, Java, C#, TypeScript, Python, and Ruby analyzers
* Improve PHP nullable type extraction (e.g., `?string` now correctly captured)
* Update CONTRIBUTING.md with TreeSitterHelper usage guidelines