* Fix C++ analyzer crash on template forward declarations without type bodies (e.g., `template<typename T> class Foo;`)
* Clarify C/C++ spec: template forward declarations without bodies are skipped during type extraction
